{
  "name": "e2e",
  "version": "1.0.0",
  "description": "GoBus Playwright end-to-end tests (fixture-backed, no network)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2"
  }
}
